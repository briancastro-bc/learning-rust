#[allow(dead_code)]
enum Sort {
    Shell,
    Bubble,
    Radix,
}

impl Sort {
    pub fn new() -> () {}
}

pub fn shell_sort() -> Vec<i32> {
    let mut vector = Vec::new();
    vector.insert(0, 389);
    vector.insert(1, 456);
    return vector;
}
